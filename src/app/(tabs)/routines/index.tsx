import { Text, View } from 'react-native';

export default function RoutinesRoute() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: '#fff' }}>📋 Seus Blocos de Treino (A, B, C)</Text>
    </View>
  );
}